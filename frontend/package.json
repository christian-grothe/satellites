{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.worklet.json && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/audioworklet": "^0.0.100",
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "osc-js": "^2.4.1"
  }
}
